{
  "name": "@tessera/importer",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --build tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test:unit": "vitest run",
    "import:airtable": "tsx src/cli.ts",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@tessera/fields": "*",
    "@tessera/types": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  },
  "files": ["dist"]
}
