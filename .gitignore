node_modules/
dist/
build/
.next/
out/
coverage/
.turbo/
*.tsbuildinfo

.env
.env.local
.env.*.local
!.env.example

*.log
npm-debug.log*
.DS_Store
Thumbs.db

.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.recommended.json

/playwright-report/
/test-results/
/blob-report/
.playwright/

/infra/compose/volumes/
/tmp/
/uploads/

# Portable local database used when Docker is unavailable (scripts/local-postgres.mjs)
.local/

# Production build output, kept separate so it does not collide with the dev server.
.next-build/

# Production secrets for the compose stack. Generated on the server, never committed: this file
# holds the database password and the signing key that every attachment link depends on.
.env.prod
infra/compose/.env.prod

# Build output for a cPanel upload. Regenerated by scripts/build-cpanel-bundle.mjs; it carries a
# full node_modules and has no business in version control.
dist-cpanel/
